# ============================================================
# GenRetrieval - Core dependencies (installable, cross-platform)
# ------------------------------------------------------------
# IMPORTANT: keep this file PURE ASCII.
#   pip on Windows reads requirement files with the system locale
#   codec (GBK on zh-CN). Non-ASCII comments raise:
#     UnicodeDecodeError: 'gbk' codec can't decode byte 0x94 ...
# ------------------------------------------------------------
# torch / torchvision are NOT listed here. Install them separately
# according to your CUDA version, BEFORE this file:
#
#   Linux / cloud (torch pulled from the pytorch index, deps from mirror):
#     pip install torch==2.6.0 torchvision==0.21.0 \
#         --index-url https://download.pytorch.org/whl/cu118 \
#         --extra-index-url https://mirrors.cloud.tencent.com/pypi/simple/
#
#   Windows (from local wheels in .wheels\, deps from mirror):
#     pip install .wheels\torch-2.6.0+cu118-*.whl ^
#                 .wheels\torchvision-0.21.0+cu118-*.whl ^
#         -i https://mirrors.cloud.tencent.com/pypi/simple/
# ------------------------------------------------------------
# Packages from the original requirements.txt that are NOT included,
# because they cannot be installed on Windows or are unused here:
#   torchrec / fbgemm_gpu  -> no Windows wheel, source build required
#   triton                 -> Linux only
#   deepspeed              -> needs a full CUDA toolkit on Windows
#   nvidia-*-cu11/cu12     -> Linux only; the Windows torch wheel
#                             already bundles the CUDA runtime
#   torchsnapshot / torcheval / pulsar-client -> unused by this project
# ============================================================

# --- LLM training stack ---
transformers==4.57.1
trl==0.24.0
peft==0.14.0
accelerate==1.10.1
datasets==4.2.0
bitsandbytes==0.48.1
huggingface-hub==0.35.3
tokenizers==0.22.1
safetensors==0.6.2
sentencepiece

# --- Scientific stack ---
# numpy MUST stay < 2: torch 2.6 and pandas 2.2 are built against the 1.x ABI.
numpy==1.26.3
pandas==2.2.2
scipy==1.14.0
pyarrow==21.0.0

# --- Retrieval / SID construction ---
faiss-cpu
POT
# rq/models/layers.py uses sklearn.cluster.KMeans for codebook init.
scikit-learn

# --- Utilities ---
einops
wandb
tqdm
PyYAML
protobuf
